# ReAct Multi-Agent System Requirements
# LangChain and LangGraph for multi-agent orchestration

langchain>=0.1.0
langgraph>=0.0.20
langchain-community>=0.0.10
pydantic>=2.0.0

# Note: SLM API client and other tools are reused from slm_agent/
# No additional dependencies for RAG (ChromaDB, FAISS) - not needed
